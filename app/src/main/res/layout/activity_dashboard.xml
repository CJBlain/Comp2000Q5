<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!-- First Row: Two Square Buttons Side by Side -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="18dp">

        <!-- View Details Button -->
        <Button
            android:id="@+id/buttonViewDetails"
            android:layout_width="0dp"
            android:layout_height="150dp"
            android:layout_weight="3"
            android:text="View My Details"
            android:layout_marginEnd="8dp"
            android:background="@android:color/holo_blue_light" />

        <!-- Edit Details Button -->
        <Button
            android:id="@+id/buttonEditDetails"
            android:layout_width="0dp"
            android:layout_height="150dp"
            android:layout_weight="3"
            android:text="Edit My Details"
            android:layout_marginStart="8dp"
            android:background="@android:color/holo_green_light" />
    </LinearLayout>

    <Button
        android:id="@+id/buttonManageHoliday"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Manage Holidays And Booking"
        android:layout_marginBottom="168dp"
        android:padding="20dp" />

    <!-- Notification Button -->
    <Button
        android:id="@+id/buttonNotification"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@drawable/circular_button"
        android:text="Notifications"
        android:layout_gravity="center"
        android:textColor="@android:color/white" />
</LinearLayout>